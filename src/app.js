let app = {
    init: function() {
        document.querySelector('#js-app-apply').addEventListener('click', this.apply, false);
    },
    apply: function() {
        alert('not implemented yet');
    }
}
app.init();